import { FC } from 'react';

interface PropsFormSubmit {}

const FormSubmit: FC<PropsFormSubmit> = (props) => {
    const {} = props;

    return (
        <div>
            
        </div>
    );
};

export default FormSubmit;